Run the Material Standalone example first because it will place a necessary file in the the Materials directory
Run this file from the APE root directory
This shows only a way of forcing the material tracking object into APE. 